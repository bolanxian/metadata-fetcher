{
  "name": "metadata-fetcher",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "onFail": "error"
    }
  },
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "coverage": "vitest --coverage.enabled --coverage.provider=istanbul --coverage.reportsDirectory=../__coverage__",
    "dev": "vite",
    "preview": "vite preview",
    "preview:pages": "vite preview --outDir ../dist-pages",
    "build": "vue-tsc --noEmit && bun run build:client && bun run build:server",
    "build:min": "vue-tsc --noEmit && bun run build:client -- --minify && bun run build:server -- --minify",
    "build:client": "vite build",
    "build:server": "vite build --ssr main.ssr.ts",
    "build:koishi": "export VITE_TARGET=koishi && bun run build:client --",
    "build:pages": "export VITE_TARGET=pages && bun run build:client --",
    "build:pages:min": "bun run build:pages -- --minify",
    "crate": "bun run crate:svg && bun run crate:reg && bun run crate:tray",
    "crate:svg": "cd crates/convert-svg && cargo build --release && mv -f target/release/convert-svg.exe ../../node_modules/.bin/",
    "crate:reg": "cd crates/reg-utils && cargo build --release && mv -f target/release/reg-utils.exe ../../dist/",
    "crate:tray": "cd crates/tray && cargo build --release && mv -f target/release/tray.dll ../../dist/",
    "convert:png": "cat ./public/favicon.svg | convert-svg png > ./dist/favicon.png",
    "convert:ico": "cat ./public/favicon.svg | convert-svg ico > ./dist/favicon.ico",
    "tar": "tar cf metadata-fetcher.tar LICENSE\\* deno.json \\*.md run.bat lib/\\*.ts dist/\\*",
    "zip": "7z a metadata-fetcher.zip -- LICENSE\\* deno.json \\*.md run.bat lib/\\*.ts dist/\\*",
    "7z": "7z a metadata-fetcher.7z -m0=LZMA2 -mx=9 -mhc=off -ms=on -mqs=on -- LICENSE\\* deno.json \\*.md run.bat lib/\\*.ts dist/\\*"
  },
  "dependencies": {
    "@vue/runtime-dom": "^3.5.42",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "bind-script": "github:bolanxian/BindScript",
    "cheerio": "^1.2.0",
    "code-128-encoder": "^3.1.1",
    "datamatrix-svg-ts": "^1.0.2",
    "lru-cache": "^11.5.2",
    "qrcode": "^1.5.4",
    "temporal-polyfill": "^0.3.2",
    "view-ui-plus": "^1.3.24",
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/deno": "^2.7.0",
    "@types/node": "^25.9.5",
    "@types/qrcode": "^1.5.6",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vitest/coverage-istanbul": "^4.1.11",
    "@vitest/ui": "^4.1.11",
    "koishi": "^4.18.11",
    "rollup": "^4.63.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vitest": "^4.1.11",
    "vue-tsc": "^3.3.11"
  },
  "workspaces": [
    "koishi-plugin"
  ]
}