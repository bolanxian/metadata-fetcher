{
  "name": "metadata-fetcher",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "start": "start deno run --quiet --no-prompt --no-remote -S -E -N -R -W=./__cache__ --allow-run --unstable-ffi --allow-ffi ./lib/main.ts start",
    "dev": "vite",
    "preview": "vite preview",
    "preview:pages": "vite preview --outDir ../dist-pages",
    "build": "vue-tsc --noEmit && npm run build:client && npm run build:server",
    "build:client": "vite build",
    "build:server": "vite build --ssr main.ssr.ts",
    "crates": "npm run crate:reg && npm run crate:tray",
    "crate:reg": "cd crates/reg-utils && cargo build --release && move /Y target\\release\\reg-utils.exe ..\\..\\dist\\",
    "crate:tray": "cd crates/tray && cargo build --release && move /Y target\\release\\tray.dll ..\\..\\dist\\",
    "extra:png": "python ./lib/convert-svg.py ./public/favicon.svg ./dist/favicon.png",
    "extra:ico": "ffmpeg -i ./dist/favicon.png ./dist/favicon.ico",
    "tar": "tar cf metadata-fetcher.tar deno.json *.md run.bat lib/*.ts dist/*",
    "7z": "7z a metadata-fetcher.7z -m0=LZMA2 -mx=9 -mhc=off -ms=on -mqs=on -- deno.json *.md run.bat lib/*.ts dist/*"
  },
  "dependencies": {
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/xterm": "^5.5.0",
    "bind-script": "github:bolanxian/BindScript",
    "cheerio": "^1.2.0",
    "lru-cache": "^11.2.5",
    "qrcode": "^1.5.4",
    "temporal-polyfill": "^0.2.5",
    "view-ui-plus": "^1.3.21",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@types/bun": "^1.3.7",
    "@types/deno": "^2.5.0",
    "@types/node": "^20.19.30",
    "@types/qrcode": "^1.5.6",
    "@vitejs/plugin-vue": "^6.0.3",
    "koishi": "^4.18.10",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vue-tsc": "^3.2.4"
  },
  "workspaces": [
    "koishi-plugin"
  ]
}